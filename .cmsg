fix(publish): npm view 查不到版本时不得中止脚本（set -e 陷阱）

幂等发布代码用 EXISTING_SIZE=$(npm view ...) 捕获输出，但版本不存在时
npm view 返回非零；在 set -euo pipefail 下**赋值语句自身失败**并中止脚本，
导致「首次发布必然在认证后静默终止」（实测 v0.1.3-BETA.2 发布）。

给 npm view / npm pack 两处命令替换补 `|| true`，并归一化空白字符。